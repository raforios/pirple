/** 
 * 
 * Token data model
 * 
*/

// Dependencies

// Container for the module (to be exported)
let token = {};

// Definition
token.model = ( (  ) => {
    this.id = id;
    this.phone = phone;
    this.expires = expires;
});

// Export the module
module.exports = token;
